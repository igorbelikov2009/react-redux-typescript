import React, { FC } from "react";
import { Container, Row } from "react-bootstrap";

const Main: FC = () => {
  return (
    <Container>
      <Row>
        <h3>Выбери страницу в навбаре</h3>
      </Row>
    </Container>
  );
};

export default Main;
